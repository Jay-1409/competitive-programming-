#include <stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    // for(int i=1;i<=t;i++){
        int n=0;
        scanf("%d",&n);
        int s[n];
        fgets(s,n,stdin);
        puts(s);

//    }
}