#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
int main() {
    int t=1;
    // scanf("%d",&t);
    int ans=10;
    while (t--) {
        ans+=0;
        while(t--) {
            printf("%d \n",ans);
        }
    }
    return 0;
}